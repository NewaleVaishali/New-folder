import { Form, Modal, Button } from "semantic-ui-react";
export default function UpdateStoreForm() {
    return(
        <Modal
        trigger={<Button primary >Update Store</Button>}
      >
        </Modal>
    );
}